-- generated by cds-compiler version 2.7.0
COLUMN TABLE slah_db_typAnrede (
  name NVARCHAR(255),
  descr NVARCHAR(1000),
  code NVARCHAR(5000) NOT NULL,
  PRIMARY KEY(code)
) WITH ASSOCIATIONS (
  MANY TO MANY JOIN slah_db_typAnrede_texts AS texts ON (texts.code = code),
  MANY TO ONE JOIN slah_db_typAnrede_texts AS localized ON (localized.code = code AND localized.locale = SESSION_CONTEXT('LOCALE'))
)